# RuneForge
Rune Forge is a modular Java script loader with a portable Windows launcher. The loader provides a small script API and keeps individual automation modules isolated from the core runtime. Scripts can be added or removed without rebuilding the loader.    The portable launcher manages its own Java runtime and keeps loader data, scripts.
