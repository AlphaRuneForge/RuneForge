# First push

Extract the source package into the root of your cloned GitHub repository.

```powershell
git status
git add -A
git commit -m "Initial Rune Forge release"
git push origin main
```

Do not commit downloaded runtimes, logs, user data, or third-party client
binaries.
